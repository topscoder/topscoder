#!/bin/bash

source utils.sh

start_block "Running .conf-git"

# git config
git config --global user.name "topscoder"
git config --global user.email "86197446+topscoder@users.noreply.github.com"

finish_block ".conf-git"